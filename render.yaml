services:
  - type: web
    name: snapload-digital
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: .
    routes:
      - type: redirect
        source: /deposit
        destination: https://stripe.com
        status: 302
